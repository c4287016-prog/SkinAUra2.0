<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">

        <!-- Home -->
        <item
            android:id="@+id/nav_home"
            android:title="Home"
            android:icon="@drawable/home"
            android:iconTint="#000000"
            android:titleTextColor="#000000"  />

        <!-- Divider -->
        <item
            android:id="@+id/divider1"
            android:title="--------"
            android:enabled="false" />

        <!-- Skin Types -->
        <item
            android:id="@+id/nav_oily"
            android:title="Oily Skin"
            android:titleTextColor="#000000" />
        <item
            android:id="@+id/nav_combination"
            android:title="Combination"
            android:titleTextColor="#000000" />
        <item
            android:id="@+id/nav_dry"
            android:title="Dry"
            android:titleTextColor="#000000" />
        <item
            android:id="@+id/nav_sensitive"
            android:title="Sensitive"
            android:titleTextColor="#000000" />

        <!-- Divider -->
        <item
            android:id="@+id/divider2"
            android:title="--------"
            android:enabled="false" />

        <!-- Info -->
        <item
            android:id="@+id/nav_about"
            android:title="About Us"
            android:icon="@drawable/baseline_info_24"
            android:iconTint="#000000"
            android:titleTextColor="#000000" />
        <item
            android:id="@+id/nav_contact"
            android:title="Contact Us"
            android:icon="@drawable/outline_call_24"
            android:iconTint="#000000"
            android:titleTextColor="#000000" />

        <!-- Divider -->
        <item
            android:id="@+id/divider3"
            android:title="--------"
            android:enabled="false" />

        <!-- Logout -->
        <item
            android:id="@+id/nav_logout"
            android:title="Logout"
            android:icon="@drawable/outline_logout_24"
            android:iconTint="#000000"
            android:titleTextColor="#000000" />

    </group>
</menu>
